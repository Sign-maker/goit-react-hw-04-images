export const INFO_TYPES = {
  error: 'error',
  notification: 'notification',
};

export const INIT_REQUEST_PARAMS = {
  perPage: 12,
};

export const STATUS = {
  idle: 'idle',
  pending: 'pending',
  resolved: 'resolved',
  rejected: 'rejected',
};
